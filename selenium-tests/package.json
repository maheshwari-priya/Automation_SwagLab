{
  "name": "swaglab-selenium-tests",
  "version": "1.0.0",
  "description": "Selenium WebDriver automation for SauceDemo (Swag Labs)",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 60000 --reporter mochawesome --reporter-options reportDir=reports,reportFilename=selenium-report"
  },
  "dependencies": {
    "selenium-webdriver": "^4.27.0"
  },
  "devDependencies": {
    "mocha": "^10.7.3",
    "mochawesome": "^7.1.3"
  }
}
